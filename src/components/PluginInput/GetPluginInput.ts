import React from 'react';
// possible input getter stub.
const getPluginInput = (mimeType: string) => {
  switch (mimeType) {
    case 'text/plain':
      break;
    case 'image/png':

    case 'image/jpg':

    case 'application/json':

    case 'text/html':
    default:
      break;
  }
};

export default getPluginInput;
